One-line: Circular avatar (initials or photo) for attorneys, investigators, and clients.

```jsx
<Avatar name="Geoff Berg" size={48} tone="navy" />
<Avatar name="Client" src="/photo.jpg" />
```
Falls back to initials when `src` is absent.
