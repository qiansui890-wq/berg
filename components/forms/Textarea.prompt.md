One-line: Multi-line text field for case descriptions and notes; matches Input's focus ring.

```jsx
<Field label="Describe what happened" required>
  <Textarea rows={5} placeholder="When did you first notice…" />
</Field>
```
Props: `invalid`, `disabled`, `rows`. Vertically resizable.
