/* Berg PC — Supabase config.
   Copy your project's values from: Supabase dashboard → Project Settings → API.
   The anon key is safe to expose in the browser; row-level security protects data. */
window.BERG_SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
window.BERG_SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
